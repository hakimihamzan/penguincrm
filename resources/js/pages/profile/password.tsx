import ProfileLayout from '@/layouts/app/profile-layout';

function Password() {
    return (
        <ProfileLayout>
            <div>Password</div>
        </ProfileLayout>
    );
}

export default Password;
